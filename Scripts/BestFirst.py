from Search import Search
class BestFirst(Search): # takes into account only h(n)
    def __init__(self):
        pass
    def extract():#We have to extract the one with the lowest f(n) = h(n)
        pass
    def insert():#We have to insert taking into account f(n) as well (we call it h which is the priority the heuristic)
        pass